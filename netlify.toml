[build]
  publish = "."

# Run the auth gate (PIN preview lock) on every request.
# The PIN and session secret are set as Netlify env vars (CORRECT_PIN,
# SESSION_SECRET). To make the site public, comment out the block below.
[[edge_functions]]
  path = "/*"
  function = "auth"
